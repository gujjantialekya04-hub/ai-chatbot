[project]
name = "generative-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gujjanti alekya", email = "gujjantialekya04@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.3.18",
]

[project.scripts]
generative-ai = "generative_ai:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
